name: Foforecast Github actions
pull_request:
  branches: [ master ]
jobs:
  Windguru-Crawler:
    runs-on: ubuntu-latest
      steps:
        - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
        - name: Check out repository code
          uses: actions/checkout@v2
